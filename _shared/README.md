# Shared Utilities

This directory contains shared scripts and configuration files used across the various example projects.

## Contents

- **`bin/`**: Executable scripts added to the `PATH` via `direnv`.
